[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "amd"
version = "0.1.0"
description = "Adaptive Markdown artifacts for agents"
requires-python = ">=3.11"

[project.scripts]
amd = "amd.cli:main"

[tool.setuptools]
packages = ["amd"]
